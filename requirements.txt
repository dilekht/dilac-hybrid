# Knowledge-based track (local, CPU)
numpy>=1.24
scipy>=1.10
openpyxl>=3.1
matplotlib>=3.7

# Hybrid track (GPU; versions pinned to what Kaggle/Colab provide)
torch>=2.1
transformers>=4.40
tqdm>=4.65
